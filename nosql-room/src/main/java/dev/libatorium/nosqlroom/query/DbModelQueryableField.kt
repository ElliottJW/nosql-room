package dev.libatorium.nosqlroom.query

/**
 * Represents a single queryable field for a model, demonstrated as a key-value pair.
 */
data class DbModelQueryableField(val key: String, val value: String)
